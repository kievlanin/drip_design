# Поточний стан проєкту DripCAD / Drip Designer Pro

**Призначення цього файлу:** швидкий огляд структури та запуску після змін у середовищі (перейменування папки, новий ПК, інший Python). **На початку сесії** переглядайте його, **наприкінці** — оновлюйте зміст і дату внизу. Рішення з роботи з AI теж коротко фіксуйте тут; код зберігайте на диск (`Ctrl+S`), за можливості робіть commit — так контекст не залежить лише від історії чату.

**Репозиторій:** підключено до **GitHub** (`origin`); перед push — `git pull` / вирішення конфліктів за потреби.

---

## Ключові файли для швидкого доступу («улюблені» в IDE)

Закріпіть у Cursor/VS Code (pinned tabs / швидкий перехід) разом із цим файлом і [PROJECT_CONTEXT.md](PROJECT_CONTEXT.md):

| Файл | Навіщо |
|------|--------|
| `main_app/main.py` | Точка входу |
| `main_app/ui/dripcad_legacy.py` | Полотно, магістраль, поливи, подвійний ЛКМ (труба / споживач) |
| `main_app/ui/app_ui.py` | `DripCADUI`, оркестратор, прогрес, SRTM |
| `main_app/ui/control_panel_impl.py` | Панель, розклад поливів, вкладка **«Магістраль (HW)»** (розрахунок, труби магістралі; старі **H пікет / Vmax≥** прибрані) |
| `main_app/ui/map_viewer_tk_window.py` | Вкладка «Карта», ліва колонка, SRTM |
| `main_app/ui/map_left_draw_widgets.py` | Ліва панель: вкладки **Малювання / Магістраль** (траса, збереження графа; **LBL** — підписи сабмейну + телескоп магістралі) |
| `main_app/ui/silent_messagebox.py` | Діалоги без системного звуку |
| `main_app/io/file_io_impl.py` | Збереження/завантаження проєкту, KML/PDF |
| `main_app/io/project_serialization.py` | JSON-safe sanitize, atomic write, readable JSON decode errors, trunk hydro cache key restore |
| `main_app/io/project_normalizers.py` | Нормалізація payload-ів зі зворотною сумісністю (`consumer_schedule` тощо) |
| `main_app/io/project_blocks.py` | Save/restore `field_blocks_data` runtime geometry ↔ JSON |
| `main_app/io/project_trunk.py` | Підготовка trunk payload для збереження (`sections` / `telescoped_sections`, allowed pipes, hydro cache) |
| `modules/hydraulic_module/trunk_irrigation_schedule_hydro.py` | Розрахунок магістралі за слотами поливу; slack ΔH для телескопа на ребрі; **`estimate_min_pump_head_m_uniform_largest_allowed_pipe`** (оцінка H при «напір = 0»); плоский список дозволених труб без імпорту `hydraulics_core` |
| `modules/hydraulic_module/api.py` | Публічний façade для UI-facing hydraulic helper-ів; UI не має імпортувати приватні helper-и напряму з `hydraulics_core` |
| `modules/hydraulic_module/pipe_weight_optimizer.py` | Оптимізація Ø/телескоп (money/weight); порядок секцій **апстрім → тонше** |
| `modules/hydraulic_module/trunk_tree_compute.py` | HW по дереву магістралі |
| `modules/hydraulic_module/trunk_map_graph.py` | Граф магістралі: валідація, пари ребер, орієнтація, **`ensure_trunk_node_ids`** (дедуп дубль-`id`) |
| `tests/test_trunk_map_graph.py` | Тести топології магістралі (у т.ч. дублікати `T9`→`T10`) |
| `designs/test01/test01.json` | Зразок проєкту з магістраллю (узгоджені id після правок пікетів) |

---

## Що це за проєкт

Desktop-додаток на **Python + Tkinter** для проєктування крапельного зрошення: креслення поля (у т.ч. кілька окремих **блоків**), сабмейни, авто- та ручні латералі, гідравлічний розрахунок (Hazen–Williams), рельєф/ізолінії, локальні SRTM-тайли та експорт KML/DXF/PDF. Окремо від основного вікна (меню **Інструменти**) — польовий калькулятор латераля та підбір телескопа сабмейну за бюджетом напору. Детальніше — у [PROJECT_CONTEXT.md](PROJECT_CONTEXT.md).

---

## Точка входу (як запускати)

З кореня репозиторію:

```text
py main_app/main.py
```

або

```text
python -m main_app.main
```

У `main_app/main.py` корінь проєкту додається в `sys.path`; **робочі шляхи до файлів** задаються через `main_app/paths.py` (`PROJECT_ROOT`, `PIPES_DB_PATH`, `DESIGNS_DIR`, `SRTM_DIR`) — **не** через `os.chdir`, щоб не залежати від `cwd` IDE.

**Важливо про Python на Windows:** якщо команда `python` вказує на MSYS/MINGW без `pip` і без `shapely`, використовуйте **`py`** (Windows Python Launcher) — зазвичай там уже є потрібні пакети (`shapely`, `fpdf2`, `Pillow`).

---

## Структура коду (після модульного розділення)

| Шлях | Роль |
|------|------|
| `main_app/main.py` | Запуск, `sys.path`, перевірка збігу з `paths.PROJECT_ROOT` |
| `main_app/paths.py` | `PROJECT_ROOT`, `PIPES_DB_PATH`, `DESIGNS_DIR`, `SRTM_DIR` (`_srtm_`); **`load_earthdata_credentials_from_project_file()`** — `EarthData.txt` (.netrc) → `EARTHDATA_USER` / **`EARTHDATA_USERNAME`** / `EARTHDATA_PASSWORD` |
| `main_app/ui/app_ui.py` | `DripCADUI` — UI + Orchestrator (топографія, розрахунок, BOM, SRTM у потоці) |
| `main_app/ui/dripcad_legacy.py` | `DripCAD` — canvas, події, **field_blocks**, рельєф, гідравлічне відображення, snap сабмейну, графіки |
| `main_app/ui/control_panel_impl.py` | Панель керування |
| `main_app/ui/control_panel.py` | Реекспорт `ControlPanel` |
| `main_app/io/file_io_impl.py` | Основний entrypoint JSON load/save + KML/DXF/PDF; збереження лише під **`designs/<проєкт>/`** (`ensure_project_dir`, санітизація імені); **«Зберегти як»** — завжди `designs/<ім’я>/<ім’я>.json` + `pipes_db.json` |
| `main_app/io/project_serialization.py` | Технічні JSON helpers: sanitize, atomic write, форматування JSON-помилки, відновлення int-ключів trunk hydro cache |
| `main_app/io/project_normalizers.py` | Payload normalizers зі зворотною сумісністю; зараз `consumer_schedule` (`nodes` legacy, `cost_index` → `money`, clamping, source mode) |
| `main_app/io/project_blocks.py` | `field_blocks_data` save/restore: `LineString` runtime geometry ↔ JSON координати, legacy rings-only payload |
| `main_app/io/project_trunk.py` | Підготовка trunk JSON payload перед save: вузли, ребра, `sections` / `telescoped_sections`, `trunk_allowed_pipes`, hydro cache |
| `main_app/ui/map_viewer_tk_window.py` | Вкладка «Карта»: `tkintermapview`, **ліва колонка** — вертикальний **`PanedWindow`**: **зверху** блок **«Зона / тайли / шари»** (без вкладок); **знизу** окрема **«Панель малювання»** з **`ttk.Notebook`** лише на **2 вкладки** — **«Малювання»** (режими/дії) та **«Магістраль»** (траса); оверлеї, межі кешу, `geo_ref`, прев’ю чернеток, лінійка |
| `main_app/ui/tooltips.py` | Спільні тултіпи: `attach_tooltip` (панель), `attach_tooltip_dark` (карта) |
| `main_app/io/file_io.py` | Реекспорт |
| `main_app/orchestrator.py` | Geo / Hydraulic / BOM / stress-test; `build_contours(..., elevation_points=?, progress_cb=?, interp_method=?)` |
| `main_app/contracts/orchestrator_models.py` | Typed snapshots для `last_hydraulic` / `last_bom` / `last_stress`; назовні зберігається сумісний `dict` |
| `modules/geo_module/topography_core.py` | `TopoEngine`: ізолінії (marching squares, обрізка Polygon), SRTM-сітка; сітка Z — **IDW** або **кріггінг**; **згладжування сітки Z** перед marching squares (`_CONTOUR_GRID_Z_SMOOTH_PASSES`); `_fetch_earthdata_batch` підхоплює креди з **`load_earthdata_credentials_from_project_file`**; рівні **k×крок** / **`fixed_z_levels`** |
| `modules/geo_module/srtm_tiles.py` | Завантаження 1°×1° тайлів: **Skadi**, **Earthdata** (власний URL або **`earthaccess` + SRTMGL1**), **`configure_earthdata_tk_bridge`** + діалог логіну; межі тайлів на карті |
| `modules/geo_module/engine.py` | `GeoModule` |
| `modules/hydraulic_module/__init__.py` | Лінивий імпорт `HydraulicModule` (`__getattr__`) — імпорт `lateral_drip_core` без Shapely |
| `modules/hydraulic_module/api.py` | Публічний hydraulic façade для UI (`HydraulicEngine`, allowed pipe helpers, `pn_sort_tuple`) |
| `modules/hydraulic_module/lateral_drip_core.py` | 1D латераль (HW, shooting) **без Shapely** |
| `modules/hydraulic_module/lateral_solver.py` | Геометрія Shapely + реекспорт ядра |
| `modules/hydraulic_module/lateral_field_compute.py` | API польового розрахунку: `LateralFieldInput` / `compute_lateral_field` |
| `modules/hydraulic_module/manifold_block_coupling.py` | Ближній/дальній тап на нозі магістралі (одночасний полив) |
| `modules/hydraulic_module/submain_telescope_opt.py` | Підбір DN телескопа сабмейну під бюджет ΔH і `pipes_db` |
| `modules/hydraulic_module/hydraulics_core.py` | `HydraulicEngine`: секції сабмейну, **submain_profiles**, емітери по латералях |
| `modules/hydraulic_module/trunk_tree_compute.py` | **Магістраль-деверо (стійкий режим):** `TrunkTreeSpec` / `compute_trunk_tree_steady` — підсумовування Q з локальних відборів `consumption` + піддерев, HW по ребрах, опційно `dz_m` на ребрі; `validate_trunk_tree` для перевірок топології. Тести: `tests/test_trunk_tree_compute.py` |
| `modules/hydraulic_module/trunk_map_graph.py` | Граф магістралі з UI: вузли/сегменти, валідація дерева, **`expand_trunk_segments_to_pair_edges`** (канон: один сегмент = одне ребро), **`build_oriented_edges`**, **`ensure_trunk_node_ids`** (унікальні `id`, дедуп після редагування). Тести: `tests/test_trunk_map_graph.py` |
| `modules/hydraulic_module/trunk_irrigation_schedule_hydro.py` | **Поливи по магістралі:** сценарії `irrigation_slots`, кеш для кольорів сегментів; опційно на вузлі **`trunk_schedule_q_m3h`**, **`trunk_schedule_h_m`** (індивідуальні Q / цільовий напір) |
| `main_app/ui/silent_messagebox.py` | **`silent_showinfo` / `showwarning` / `showerror` / `askyesno`** замість `tkinter.messagebox` (без MessageBeep на Windows) |
| `modules/hydraulic_module/engine.py` | `HydraulicModule` |
| `modules/bom_module/engine.py` | `BOMModule` |
| `lateral_field_calculator.py` (корінь) | Tk: один крило, опція «дальнє поле на магістралі» |
| `submain_telescope_calculator.py` (корінь) | Tk: сегменти L + Q, напір насос/кінець, телескоп |

Папка **legacy** (старі копії) у репозиторії не використовується імпортами — орієнтуйтесь на таблицю шляхів вище.

---

## Дані та проєкти

- `designs/<Назва_проєкту>/` — JSON проєкту (`<Назва>.json`) + копія `pipes_db.json` у **ті самій** теці. Усі стандартні збереження та **«Зберегти проект як»** за замовчуванням потрапляють сюди (не на довільний шлях з діалогу). Ім’я теки/файлу санітизується під обмеження Windows.
- `pipes_db.json` у корені — глобальна база труб (шаблон).
- `_srtm_/` — кеш повних файлів `*.hgt` (розпаковані); у `.gitignore` ігнорується вміст, лишається `_srtm_/.gitkeep`.

**JSON проєкту:** окрім `points`, `geo_ref`, `topo_points`, `calc_results`, `params`, `allowed_pipes`:

- **`field_blocks_data`** — список блоків поля; кожен елемент містить `ring`, опційно `edge_angle`, `submain` (полілінії сабмейнів), `auto` / `manual` (координати латералів), **`params`** (словник знімка налаштувань блоку: крок ліній/емітерів, max довжина, групування рядів, опційно поля емітерів). У старих файлах без `params` використовується fallback на глобальні поля після завантаження.
- **`scene_lines`** — список декоративних поліліній (локальні XY), ескіз/ситуація; не використовується в розрахунках.
- **`trunk_map_nodes`** / **`trunk_map_segments`** — магістраль на карті/полотні: вузли (`kind`: source, valve, bend, junction, consumption; координати `x`,`y`, опційно `lat`,`lon`; у споживачів опційно **`trunk_schedule_q_m3h`**, **`trunk_schedule_h_m`** для сценарію «магістраль за поливами»), сегменти як **ребра** (два індекси в `node_indices` + **`path_local`** — полілінія труби в метрах; опційно **`pipe_material`**, **`pipe_pn`**, **`pipe_od`** після діалогу труби). Вузол `consumption` може бути як листом, так і проміжним вузлом у ланцюгу відборів. Після завантаження/завершення траси нормалізація до пар ребер у `file_io_impl.py` / `DripCAD.normalize_trunk_segments_to_graph_edges`.
- **`consumer_schedule`:** **`max_pump_head_m`** — напір насоса для розрахунку магістралі за поливами; **0** означає запит на **автопідстановку** орієнтовного мінімуму (найтовстіша дозволена труба на всіх ребрах, див. `estimate_min_pump_head_m_uniform_largest_allowed_pipe`); після успішної підстановки в полі й у JSON зберігається число **1…400**. **`trunk_display_velocity_warn_mps`** (0–8 м/с) — legacy-поріг **підсвітки** v на магістралі, зберігається в JSON і підтримується логікою, але поле **Vmax≥** з вкладки **«Магістраль (HW)»** прибране; **`trunk_telescope_label_pos`** — збережені позиції підписів телескопа (`"segIdx:chunkIdx"` → `[x,y]` м, режим **LBL**).
- **`field_blocks`** — лише кільця для сумісності; повне відновлення — з `field_blocks_data`.

**Правило сумісності JSON:** якщо формат проєкту змінюється, старі payload-и мають лишатися завантажуваними через normalizer/fallback, а збереження пише поточний канонічний формат. Тестові проєкти в `designs/` є цінними fixtures для перевірок, тому їх не можна ламати без явної міграції.

---

## Поведінка UI (стисло)

### Режими креслення (полотно «Без карти» та вкладка «Карта»)

Перемикачі режимів і **ADD/DEL** на вкладці **«Карта»** — у **нижній частині лівої колонки**, у **панелі малювання** → вкладка **«Малювання»** (синхронізація з `mode` / `action`). Верхня частина лівої колонки — **зона / тайли / шари** (розділювач `PanedWindow`). Поле **L (м)** з **Enter** (`add_by_length`) — у **низу правої панелі** (`control_panel_impl.py`).

1. **VIEW** — простий перегляд (без редагування); режим за замовчуванням після старту та після відкриття проєкту.  
2. **DRAW** — контур блоку; ПКМ замикає.  
3. **SET_DIR** — ЛКМ у блоці, потім два ЛКМ напрямку рядів → авто-латералі цього блоку. Якщо на карті задана **зона проєкту**, після **«ОНОВИТИ СІТКУ»** ці лінії тягнуться на **всю зону проєкту** (AABB), а не обрізаються лише контуром блоку; без зони — обрізання по полігону блоку.  
4. **SUBMAIN** — сабмейн у блоці; snap до перетинів і **кінців** латералів уздовж **променя** у напрямку курсора (клавіша **S** — увімк/вимк snap).  
5. **DRAW_LAT** — ручна dripline; ПКМ — фіналізація з підрізанням/подовженням до сабмейну.  
6. **PAN** — панорама.  
7. **INFO** — після розрахунку: **ЛКМ по латералі** — графік ряду; **ЛКМ по сабмейну** — **H, Q та рельєф (ΔZ від початку)** вздовж магістралі (`submain_profiles`).  
8. **RULER** — лінійка.  
9. **CUT_LATS** — лінія зрізу авто/ручних латералів.  
10. **SUB_LABEL (LBL)** — підписи **секцій сабмейну** і **телескопа магістралі**: 1-й ЛКМ — взяти найближчий підпис, рух миші, 2-й ЛКМ — зафіксувати; для магістралі позиції в **`consumer_schedule.trunk_telescope_label_pos`**; **не** скидає повний `reset_calc` без потреби.

11. **LAT_TIP** — оцінка тиску на тупиках латераля (поле).

Кнопка **«Завершити блоки → напрямок рядів»** перед першим SET_DIR для нових контурів. **Delete/BackSpace** — чернетка контуру або останній блок; окрема кнопка — очистити всі контури.

**Чернетка контуру та розрахунок:** під час **DRAW** з незамкненим контуром ЛКМ **не** викликає `reset_calc()`; після **замикання** нового блоку (`close_polygon`) гідравліка також **не** скидається автоматично — лише коли зміни справді інвалідують результати.

### Гідравліка та креслення після розрахунку

- У розрахунок передаються **індекси блоків** по кожному сабмейну (`submain_block_idx`) і **індекс блоку на латераль** (`lateral_block_idx`); у ядро можуть йти **`e_steps` / `e_flows`** (крок і витрата емітера по кожному латералю з `params` блоку або fallback з панелі). На вкладці **«Гідравліка»** дроплісти матеріалу/PN **прибрані** з панелі: рядок-статус + кнопка відкриття вікна **вибору труб проєкту** (там же mat/PN); приховані combobox’и лишаються для сумісності з `load_project` і трейсами.
- **Фіксована к-сть секцій / кількість секцій / H на крані / «підганяти сабмейн під тиск»:** зміна цих елементів **не** викликає глобальний `reset_calc` по всіх блоках — інвалідується лише гідравліка **активного** блоку (`dripcad_legacy._invalidate_hydro_ui_active_block_or_all`); якщо блоків немає — повне скидання як раніше. Команда чекбокса «фіксована к-сть» лише вмикає/вимикає поле кількості (без подвійного скидання).
- **Малювання нового блоку:** ЛКМ у режимі `DRAW` більше не проходить через загальну гілку глобального `reset_calc`; додавання нового контуру не стирає гідравліку вже порахованих блоків.
- На вкладці гідравліки — **«Скинути»** по номеру блоку (`clear_hydro_block`): вирізаються секції/емітери цього блоку, клапани за прив’язкою до сабмейнів блоку, очищаються `section_label_pos` та **submain_profiles**.
- **Колір секцій на полотні:** при малюванні береться з **поточного `pipe_db`** за mat/PN/Ø (`dripcad_legacy._section_draw_color`), а не лише з застиглого `calc_results`, щоб зміна кольору в базі одразу відображалась після перемальовування.
- **Відображення секцій:** суміжні відрізки з однаковими mat/pn/d на одному сабмейні **зливаються** для креслення; полілінія **розбивається за поворотами** (~≥22°) — підписи **L** пропорційні геометрії шматка (одна гідравлічна секція може дати кілька підписів на прямих після кута). Ключі позицій підписів: `"{sectionIndex}_{subIdx}"` та fallback для `sub_idx==0`.
- **Чому після повороту на кресленні/у списку може змінитись d:** фізичні розрізи завжди проходять через **вершини** полілінії сабмейну; діаметр відрізка береться з **математичної зони** за **серединою** відрізка (різна Q вздовж магістралі) — деталі в коментарі в `hydraulics_core.py` біля `final_cuts`.
- **Режим `SUB_LABEL`:** перетягування підпису стало двокроковим: 1-й ЛКМ — вибір найближчого підпису (він «прилипає» до курсора), 2-й ЛКМ — фіксація нового місця; тимчасовий стан скидається через `reset_temp()`.
- **Збереження позицій підписів:** після повторного розрахунку вручну пересунуті підписи відновлюються для тих же секцій (`sm_idx + section_index + sub_idx`), без повернення на авто-позицію.
- **Колізії підписів між сабмейнами:** ключ `section_label_pos` включає `sm_idx`, тому однакові `section_index` у різних сабмейнах більше не конфліктують.
- **Звіт гідравліки:** окрема вкладка **«Результати»** з дроплістом блоку; popup-вікно «Специфікація та Гідравліка» прибрано. Для блоку показується **площа поля** за контуром `ring` (м² та га).

### Вкладка «Блок»: авто-латералі та крапельниці на мапі

- Фільтр **кожну N-ту** задає лише видимість авто-латералей після розрахунку.
- Фільтри **номер від крана** та **номер з кінця** працюють тільки коли **кожну N-ту** вимкнено; вмикання будь-якого з них автоматично вимикає **кожну N-ту**.
- **Текстовий перелік** крапельниць активного блоку з **H** поза діапазоном (мін/макс з «Гідравліка», допуск ±0,02 м як у розрахунку); нумерація латераля — глобальний порядок.
- **Маски переливу / недоливу на мапі (контури):** суцільні зовнішні контури зон без «крапель» усередині; геометрія — union буферів навколо проблемних емітераів + buffer між рядами; **перелив** додатково **зшивається з ізолінією Q = Q ном** (`var_emit_flow`) у межах блоку. Під час увімкнених масок **приховані** ізолінії «Діаграма виливу» на полотні; латералі **активного** блоку без червоного/жовтого аудиту (лише контури зон). Кеш Shapely-результату між перемальовуваннями + спрощення вершин для швидкого Canvas.
- **Крапельниці (вилив) — старий режим ізоліній:** якщо маски вимкнені, «Діаграма виливу» будує IDW-ізолінії по полю **q_emit** (може виглядати як щільна сітка точок); колір сегмента за класифікацією **H** (норма / перелив / недолив); на ізолініях виливу показуються **підписи Q** (л/г).
- Задано діапазон тиску на крапельниці: у межах діапазону — **один колір** «норма» на графіках виливу; градієнт за Q — лише **поза** діапазоном (де застосовується).

### Інше

- **Основний розрахунок (DripCADUI):** вікно прогресу з **шкалою 0–100** і **злиттям** оновлень з фонового потоку (`after(0)`), щоб на великих полях (багато латералів) смуга не «зависала» на Windows/ttk. Аналогічно для **Stress-тесту**.
- **Редактор глобальної бази труб:** зразок кольору біля поля hex; **«Зберегти в файл»** перед записом JSON підтягує поточний рядок форми (у т.ч. **колір**) у `pipe_db`; після збереження — `update_pn_dropdown(skip_reset=True)` і `redraw()` без повного скидання гідравліки. Вікно вибору дозволених труб: **одне ЛКМ** у стовпці «Вик.» перемикає ✅/❌.
- **Ізолінії:** межа обрізки — спочатку **рамка зони проєкту** (з карти, `project_zone_bounds_local`), далі **KML зони SRTM** (якщо імпортовано), інакше об’єднання блоків поля, інакше опукла оболонка за точками висоти. Побудова в **фоновому потоці** (UI не блокується); прогрес у **заголовку вікна**; кнопки **«Побудувати ізолінії»** (IDW) і **«Побудувати ізолінії (кріггінг)»** (`btn_build_contours_kriging`). Для великих площ автоматично згрублюються крок сітки та/або крок висоти (з повідомленням і `last_contour_adaptation_note` на `TopoEngine`). Висоти ізоліній — **кратні кроку з панелі** (при згрубленні — `mult × крок`). Кріггінг зазвичай дає **менш шумну** поверхню порівняно з IDW; обидва методи лишаються паралельно.
- **Меню «Інструменти»:** запуск у окремому процесі `lateral_field_calculator.py` та `submain_telescope_calculator.py` (`subprocess`, `cwd=PROJECT_ROOT`).
- **Панель малювання:** з правої панелі прибрано; на вкладці **«Карта»** — ліва колонка: **зверху зона** (тайли, контур, шари), **знизу** окрема панель з вкладками **Малювання / Магістраль** (`map_viewer_tk_window.py`, `PanedWindow`). Поле **`L (м)`** — у правій панелі над статистикою.
- **Рельєф / SRTM:** імпорт KML зони, завантаження тайлів у `_srtm_` (пропуск файлів, що вже є в кеші), «Завантажити з супутника» спочатку читає локальні `.hgt`, далі резервні API (Open-Elevation / Earthdata за режимом). **Крок сітки точок висоти** — **«Роздільна здатність»**: **5, 15, 30, 45, 90** м. **Межі кешу** на карті / «Рельєф». Зона проєкту на карті: кнопка **«Тайли»** на вкладці «Рельєф» — **лише** завантаження `.hgt` у `_srtm_` (без заливки висот у модель); висоти — окремо. **EarthData.txt** у корені (формат .netrc) або env; для **earthaccess** обов’язково узгоджені **`EARTHDATA_USERNAME`** + пароль; при помилці входу — **діалог** у головному вікні.
- **Вкладка «Карта»:** відкрита карта задає **`geo_ref`** з центру виду (якщо ще порожньо); малювання контуру поля на карті показує уже покладені сегменти та гумку; лінійка масштабу — окремий шар над картою; тултіпи кнопок режимів на карті — **зверху** (`attach_tooltip_dark`, `above=True`); більшість кнопок карти й панелі керування мають **українські tooltips** (`main_app/ui/tooltips.py`).
- **Магістраль + «Інфо»:** інструмент **Інфо** на полотні «Без карти» і на карті (потрібен `geo_ref`): при наведенні на **магістраль** підсвічується **жовтим** шлях до насоса; на **розгалуженні** — додатково **лаймом** гілки до **споживачів**. ЛКМ — діалог з підписом об’єкта (пріоритети як у `_collect_world_pick_hits`). Деталі — `dripcad_legacy.py` (`trunk_info_highlight_world_paths`, `_collect_world_pick_hits`), `map_viewer_tk_window.py` (`_paint_map_live_preview`, `<Motion>` для `map_pick_info`).
- **«Вибір» (`select`):** лише на **полотні «Без карти»** (на вкладці «Карта» інструмент маршрутизується на віджет карти). Та сама логіка підбору, що **«Інфо»**, але **після ЛКМ** вибір **залишається підсвіченим**; **рамка / кросрамка** для множинного вибору; **ПКМ** — спочатку скинути вибір, потім вийти. Код: `dripcad_legacy.py` (`handle_left_release`, `_draw_canvas_selection_layer`, `_pick_hits_in_world_rect`); кнопка з діагональною стрілкою — `map_left_draw_widgets.py`.
- **Меню Файл:** пункт **«Зберегти проект як (JSON)…»** (поруч із звичайним збереженням).
- **Гідравліка:** перемикання **бісекція / Ньютон / порівняння** для латералів **не** скидає `calc_results` по всіх блоках; оновлення після **«▶ РОЗРАХУНОК»**.
- **Магістраль за поливами:** розрахунок і кеш кольорів сегментів; **беззвучні** діалоги; **подвійний ЛКМ** по відрізку — труба з каталогу; по **споживачу** — Q/H з перерахунком; **ПКМ** по вибраній трубі — **«Вибір труби»**, **«Профіль прокладки»**, **«Графік тиску вздовж ребра»** (тільки вибране ребро і його реальний `path_local`), **«Графік тиску вздовж гілки від насоса…»** (окремий режим повної гілки). На графіку тиску **ПКМ** → **«Вставити пікет тут»** ріже відповідне ребро в позиції `s`. Режим **`trunk_pipes_selected`**, **«Скинути результат»** — лише кеш/підсвітку. Оптимізація: **телескоп 1–4 секції** на ребро, порядок секцій **від джерела товстіша → до споживача тонша** (`pipe_weight_optimizer` + креслення). **Hover Q/P** над насосом — `_trunk_hydro_hover_pick_pump`; при заданому H — **min_required_source_head_m** в підписі. Старі поля **Vmax≥** і **H для пікета @ H** з вкладки **«Магістраль (HW)»** прибрані; підписи секцій телескопа на трасі (**Ø** у тексті) + **LBL** для перетягування; **шари** тегів **`trunk_map_BOM`** / **`trunk_map_Cosmetic`**; **трикутники** на стику діаметрів. **Напір насоса (задано) = 0** у вкладці **«Розклад»** — автозаповнення оцінкою за найтовстішою дозволеною трубою (`control_panel_impl` + `dripcad_legacy.estimate_max_pump_head_from_largest_allowed_trunk_pipe`). Перед розрахунком **`sync_trunk_tree_data_from_trunk_map`**: пріоритет **`sections`** над **`telescoped_sections`** у `_trunk_non_empty_section_list_from_seg`, щоб телескоп не «губився» через розсинхрон ключів.
- **Каталог труб магістралі:** кнопка **«Труби для магістралі…»** — вкладка **«Магістраль (HW)»** (`control_panel_impl.py`).
- **Редагування магістралі на полотні/карті (VIEW):** перетягування вузлів ЛКМ (топологія ребер зберігається); конфлікт з рамкою «Вибір» знято; подвійний ЛКМ по ребру покращено (полілінійні сегменти, пріоритет за відстанню до лінії vs вузла). **Delete** не видаляє вузли/ребра графа — видалення з **ПКМ-меню** (`_open_trunk_graph_context_menu`). Снап споживача до **крана** (початок сабмейну): при постановці; після перетягування — при відпусканні; **пунктирні кола** зон снапу навколо кранів, коли релевантно (`_consumer_valve_snap_overlay_enabled`).
- **Редагування магістралі (пікети / ребра):** після додавання вузлів **`ensure_trunk_node_ids`** гарантує **унікальні** `id` у `trunk_map_nodes` (дубль `T9` у двох пікетах → другий стає `T10` тощо), щоб не ламати зв’язність для HW. Після серйозних правок **зберігайте JSON**, щоб `trunk_tree` узгодився з картою.

Дубль короткого знімка «останніх правок» — у [PROJECT_CONTEXT.md](PROJECT_CONTEXT.md), §7.

---

## Залежності (орієнтовно)

- **Обов’язково для основного CAD:** `tkinter` (зазвичай з Python), `shapely`
- **Опційно:** окремі скрипти `lateral_field_calculator.py` / частина `hydraulic_module` можуть працювати **без Shapely** (ядро `lateral_drip_core`, `lateral_field_compute`, `manifold_block_coupling`, `submain_telescope_opt`).
- **Для ізоліній кріггінгом:** `numpy`, `scipy`, `pykrige` — див. **`requirements-contours-optional.txt`** (`pip install -r requirements-contours-optional.txt`); без них кнопка кріггінгу покаже підказку про встановлення.
- **Для завантаження тайлів SRTM через NASA (earthaccess):** `earthaccess` у **`requirements-dem.txt`** (`pip install -r requirements-dem.txt` або окремо `pip install earthaccess`).
- **Для PDF-експорту:** `fpdf2`, `Pillow`

Перевірка імпортів:

```text
py -c "import tkinter, shapely; print('ok')"
```

---

## Після перейменування кореневої папки

Ім’я папки **не зашите** як абсолютний шлях: `PROJECT_ROOT` обчислюється від `main_app/paths.py`. Відкрийте нову кореневу папку як workspace і запускайте з неї.

---

## Найближчий план

### A. Поточний фундамент (блоки, BOM, редактор сабмейну)

**Модель проєкту**

- Проєкт = набір **блоків** поля; у JSON уже є `field_blocks_data[]` з `ring`, `submain`, `auto`/`manual`, **`params`** (знімок налаштувань блоку).
- **BOM** за задумом складається з:
  - BOM **кожного блоку** (латералі / крапельниці / труби блоку);
  - окремо BOM **магістралі** (сабмейн(и), арматура, труби магістралі) — у перспективі узгодити з окремою моделлю магістралі (п. B).
- Кожен блок може мати **свою** схему поливу, напрямок рядів, параметри крапельної лінії — це вже частково в `params`; потрібна узгодженість UI ↔ збереження ↔ розрахунок **в контексті обраного блоку**.

**UI вибору активного блоку**

- Дропліст **«поточний блок для проєктних робіт»** по центру панелі меню **або** окрема верхня смуга.
- Усі дії креслення/редагування, релевантні одному блоку (напрямок рядів, прив’язка сабмейну, фільтри відображення тощо), застосовуються до **обраного** блоку (де вже є `block_idx` — розширити покриття).

**Редактор сабмейну (крок паралельно з п. B)**

- Окремий режим або діалог: список **сегментів** полілінії сабмейну (довжина, мат/PN/Ø, кут, порядок).
- **Ручна корекція довжини** кожного сегмента з прив’язкою до **стандартної довжини труби** з `pipes_db` (кратність цілим числу «штанг» або кроку з каталогу; валідація сумарної довжини / координат вершин).
- Зв’язок із гідравлікою: після правок — оновлення геометрії в `field_blocks_data` і скидання/перерахунок залежних результатів.

### B. Наступний спроектований етап — **магістраль** (відкритий граф-дерево)

**Концепція**

- **Магістраль** моделюється як **дерево** (відкритий зв’язний граф без циклів), орієнтований від **витоку**:
  - **Витік (корінь)** — точка підключення до джерела / насосної станції / напору на вході мережі (задані **H₀**, **Q** або режим насоса — уточнити на етапі DTO).
  - **Точки повороту** — вершини лише зі зміною напрямку труби (топологія + геометрія на карті / у локальних координатах).
  - **Точки розгалуження** — вузли степеня ≥3: один вхідний напрямок (від батька) і кілька виходів на дочірні ділянки.
  - **Точки споживання (листя)** — підключення **навантажень**: блок(и) поля, зони поливу, клапани або агреговані вузли з відомим/заданим **графіком споживання**.
- **Ребра** — ділянки трубопроводу між вершинами: довжина, діаметр/матеріал (з `pipes_db`), опційно профіль рельєфу вздовж ділянки.
- **Графік споживання** — прив’язаний до вузла споживання (або до групи блоків): часовий ряд **Q(t)** (м³/год, л/с) або набір кроків / добовий шаблон; для розрахунку — узгодження з існуючою гідравлікою блоків (пікове Q, тривалість зон, послідовність відкриття).

**Етапи реалізації (чернетка)**

1. **Дані:** JSON/DTO для графа магістралі (`nodes[]`: type, coords, payload; `edges[]`: u, v, pipe spec, L); окремо `consumption_profiles[]` з посиланнями з листя.
2. **UI:** режим креслення / таблиця вершин; валідація дерева (один корінь, без циклів); імпорт з існуючих поліліній сабмейну як чернетка топології (опційно).
3. **Відображення:** шар на canvas поверх/поруч з блоками; підписи вузлів і напрямок від витоку.
4. **Гідравліка:** окремий розв’язувач HW по дереву з граничними умовами на листі з Q(t) або зведеним Q_max; згодом — зв’язка «листо ↔ блок field_blocks» для узгодженого Q.
5. **Звіт / BOM:** довжини і типи труб по ребрах; арматура на розгалуженнях.

**Ризики / рішення**

- Поточні **сабмейни в блоці** залишаються робочою моделлю; магістраль вводиться **паралельно** і з часом з’єднується (наприклад, лист споживання = «підключити блок N» або «підсумок сабмейнів блоку»).

**Довгостроково (після п. A–B)** — див. нижче «Що логічно доробити далі».

---

## Що логічно доробити далі

- Повністю «розпакувати» `dripcad_legacy.py` на менші модулі.
- Офіційні JSON-схеми DTO для Geo / Hydraulic / BOM.
- За потреби: узгодження підбору d на вершинах полілінії (наприклад, неподрібнення зон при «одній трубі» після повороту).
- Вбудувати `submain_telescope_opt` у повний гідравлічний прохід (сегменти з `hydraulics_core` замість ручного вводу в калькуляторі).
- Кубічна інтерполяція DEM, гідроудар — за планом v1.1 (локальні `.hgt` уже підтримуються).

---

## Нотатка сесії 2026-04-06

- Перевірено на `designs/kaharlyk/kaharlyk.json` і доведено до стабільного стану відображення прогресу розрахунку (основний + stress): бар 0–100, злиття частих UI-оновлень.
- Виправлено видимість крапельниць при «кожну N-ту»: точки рендеряться тільки на тих авто-латералях, які реально видимі за фільтром (індексне зіставлення в межах блоку).
- Доведено збереження кольору труб у глобальному редакторі: перед записом JSON застосовується поточний рядок форми (включно з `color`), додано наочний swatch.
- Додано маску фільтрації в редактор латералей і редактор труб; у редакторі крапельниць уніфіковано підпис поля як `Маска (усі поля):`.
- Поведінка маски зроблена дружньою: якщо введено звичайний текст без wildcard-символів, він автоматично трактується як підрядок (`*текст*`), тому пошук по `модель` працює очікувано.
- У редакторі латералей виправлено індексацію вибору під фільтром (стабільний `iid` = індекс у `rows`), щоб `Оновити/Видалити` працювали з правильним записом.
- Гідравліка на панелі: чекбокс/поля секцій сабмейну та обмеження на клапані більше не скидають розрахунок у **всіх** блоках — лише в активному (див. розділ «Гідравліка» вище).
- **Тестування на цей момент завершено** (фокусований ручний прохід по поточному обсягу змін). Наступні прогони — після нових доробок або за запитом. *A presto.*

## Нотатка сесії 2026-04-08

- Вкладка **«Блок»**: маски переливу/недоливу на карті (контури «хмари»), перелив зшитий з ізолінією **Q ном**; оптимізація вершин і кеш геометрії; `topography_core`: параметр **`fixed_z_levels`** для точних рівнів ізоліній.
- Оновлено **PROJECT_CONTEXT.md** / **PROJECT_STATE.md**; наступний спроектований етап зафіксовано: **магістраль** як дерево (витік, повороти, розгалуження, споживання + графіки Q(t)).

## Нотатка сесії 2026-04-10

- Карта: авто-`geo_ref`, попередній перегляд контурів і чернеток, лінійка-оверлей, тултіпи зверху, z-order прев’ю.
- Файли: «Зберегти як» і `ensure_project_dir` — лише `designs/<проєкт>/`, санітизація імені.
- Результати: площа блоку (м², га) у тексті вкладки «Результати».
- Гідравліка: без глобального скидання при зміні режиму розв’язувача латераля.

## Нотатка сесії 2026-04-10 (оновлення документації, перерва)

- Зафіксовано в репозиторії: **загальні tooltips**, **зона проєкту** та сітка латералів по зоні, **`scene_lines`** у JSON, **«Межі кешу»** на карті / узгоджений текст на «Рельєф», пропуск повторного завантаження наявних SRTM-тайлів.
- Оновлено **PROJECT_CONTEXT.md** та **PROJECT_STATE.md**; **перерва в роботі** — наступна сесія продовжить з цього знімка.

## Нотатка сесії 2026-04-10 (рельєф: IDW + кріггінг, SRTM)

- **Ізолінії:** поруч з IDW лишається **кріггінг** (PyKrige) — менш шумна поверхня; natural neighbor (MetPy) знято. `topography_core.py`: `_ordinary_kriging_to_grid`, `_roughen_ok_variogram`, `interp_method` через orchestrator / `GeoModule.build_contours`.
- **UI:** `build_contours_kriging`, `btn_build_contours_kriging`; опційні залежності — `requirements-contours-optional.txt`.
- **SRTM / DEM:** «Роздільна здатність» — **5, 15, 30, 45, 90** м.
- **Полотно:** ізолінії рельєфу — `smooth=False` у `dripcad_legacy.py`.

## Нотатка сесії 2026-04-11

- **Карта:** панель **«Малювання»** перенесена з правої панелі та з нижнього центру карти в **ліву колонку** (`create_embedded_map_panel`). Ліва колонка — **`PanedWindow`**: **зверху** постійно видима **«Зона / тайли / шари»**; **знизу** окрема **«Панель малювання»** з `Notebook` лише на **2 вкладки** — **Малювання** та **Магістраль**.
- **Малювання на карті:** повний набір режимів і ADD/DEL з LED; при зміні режиму — `reset_temp()`.
- **Магістраль:** вкладка в нижній панелі — **«Траса магістралі»** та текст про майбутню модель дерева.
- **Права панель:** нижній toolbar малювання видалено з `control_panel_impl.py`; поле **L (м)** над статистикою.

## Нотатка сесії 2026-04-11 (проміжна фіксація — магістраль, Інфо)

- **Канонічне сховище сегментів:** один елемент `trunk_map_segments` = **одне ребро** (два вузли); `expand_trunk_segments_to_pair_edges` + `normalize_trunk_segments_to_graph_edges` після траси та `load_project`; `sync` зберігає внутрішні вершини `path_local` для двовузлових ребер.
- **Інфо:** підсвітка маршрутів (жовтий → насос, лайм → споживачі від junction) на полотні та карті; `_collect_world_pick_hits` для структурного підбору.
- **Документація:** оновлено `PROJECT_CONTEXT.md` / `PROJECT_STATE.md` як **проміжну точку відновлення** (ризик обриву Інтернету / сесії). Рекомендовано **`git commit`** з цими файлами та кодом магістралі.

## Нотатка сесії 2026-04-11 (вечір — «Вибір», git, документація)

- **Інструмент «Вибір»** на полотні: постійна підсвітка після кліку, рамка (Л→П) / кросрамка (П→Л), очищення вибору та вихід з інструмента через **ПКМ**; скидання вибору при перемиканні на інструменти, відмінні від «Інфо».
- **UI:** іконка вибору — `Canvas` з діагональною стрілкою.
- **Репозиторій:** розширений `.gitignore` (в т.ч. `.venv`, кеші); перший **commit** з повідомленням **назва проєкту + дата + час**.
- Оновлено **PROJECT_CONTEXT.md** / **PROJECT_STATE.md**.

## Нотатка сесії 2026-04-12

- **GitHub:** `origin` на головну гілку; коміт зі змінами магістралі поливів, silent-діалогів, подвійного ЛКМ (труба / споживач), оновлення **PROJECT_CONTEXT.md** / **PROJECT_STATE.md** та таблиці **«улюблених»** файлів для IDE.
- **Silent messagebox:** уніфікація сповіщень без системного beep; **`silent_askyesno`** для підтверджень (очищення слотів поливу, завантаження SRTM тощо).
- **Транк поливів:** `trunk_irrigation_schedule_hydro.py` у репозиторії; індивідуальні **`trunk_schedule_q_m3h`** / **`trunk_schedule_h_m`** на вузлах споживачів.

## Нотатка сесії 2026-04-13 (магістраль, документація, перерва)

- **Магістраль:** підтверджено сценарій — видалити ребро, додати два пікети, додати нові ребра; розрахунок **працює**.
- **Код / дані:** дедуплікація `id` вузлів у `trunk_map_graph.ensure_trunk_node_ids`; приклад узгодженого проєкту — `designs/test01/test01.json`; тест — `test_duplicate_trunk_node_ids_renumbered` у `tests/test_trunk_map_graph.py`.
- Оновлено **PROJECT_CONTEXT.md** / **PROJECT_STATE.md** (знімок §7, таблиця «улюблених» файлів). **Перерва в роботі** — наступна сесія продовжить з цього знімка.

## Нотатка сесії 2026-04-14 (магістраль: вибір, пікет H, профіль)

- **Вибір/пріоритет:** при активній вкладці **«Магістраль»** пріоритет hit-test віддано trunk-об’єктам (вузли/ребра) над межами блоків; граф магістралі рендериться верхнім шаром.
- **Топологія споживачів:** `consumption` дозволено як проміжний вузол ланцюга (лист — лише коли останній у гілці); одночасність задається тільки `irrigation_slots`.
- **Автопікет за напором:** додано вставку пікета в точці `H=...` (поле вводу, default 60 м) за peak-слотом поливів; ребро розривається, pipe-параметри переносяться; додано автофокус і підсвітку нового пікета.
- **Профіль прокладки:** для вибраного сегмента труби доступний профіль земної поверхні (ламана, крок 6 м) з керованим вертикальним масштабом (default 0.1) і синхронним курсором на графіку/карті.
- **UX `select` у панелі магістралі:** ЛКМ — вибір об’єкта; ПКМ — контекстне меню. Для труби пункт **«Профіль прокладки»**. Окрему кнопку профілю прибрано.

## Нотатка сесії 2026-04-15 (гаряча фіксація перед перезапуском)

- **Оптимізація ваги труб:** додано `modules/hydraulic_module/pipe_weight_optimizer.py`; підключено для магістралі і сабмейну.
- **Магістраль (Розклад):** додано збережувані параметри `trunk_schedule_min_seg_m` та `trunk_schedule_opt_goal` (`weight` / `cost_index`) у `consumer_schedule`, UI-поля в `control_panel_impl.py`.
- **Правило L мін для магістралі:** короткий сегмент (`L < Lmin`) більше не лише попереджається — він **поглинається попереднім upstream сегментом** при підборі діаметра.
- **Сабмейн-калькулятор:** додано `Мін. довжина сегмента` + режим оптимізації `weight/cost_index`; обидва шляхи (`optimize_submain_telescope*`) враховують `min_segment_length_m`.
- **UX розкладу:** прибрано кнопку **«Скинути чернетку»**; тексти підказок оновлено на «поточний вибір» без концепції чернетки.
- **Тести:** додано/оновлено `tests/test_pipe_weight_optimizer.py` (у т.ч. кейс поглинання короткого сегмента), прогін цільових тестів — OK.

## Нотатка сесії 2026-04-15 (вечір — магістраль, UX, документація)

- **Гідравліка / оптимізація магістралі:** обмеження за швидкістю за замовчуванням вимкнено (0 = не застосовувати); у потоці розрахунку з панелі v примусово 0; прибрано поле v max з UI; фаза стиснення діаметрів у `pipe_weight_optimizer`; порожній `allowed_pipes` = жодної труби для PN; режим **`trunk_pipes_selected`** + `use_required_pump_head` у `trunk_irrigation_schedule_hydro`; кнопка **«Скинути результат»**.
- **BOM / діалог труби:** `bom_length_zero` (виключення довжини з BOM), чорний колір сегмента, пропуск у `bom_module/engine.py`, перенесення ключа в `trunk_map_graph`.
- **Карта:** оновлення гліфів вузлів при панорамуванні/зумі; **зум рамкою** узгоджено з карткою, видима рамка при drag.
- **UX:** ПКМ на вибраній трубі — **«Вибір труби»**; **hover Q/P над насосом** виправлено (`_trunk_hydro_hover_pick_pump` у `dripcad_legacy.py`).
- **Документація:** оновлено **PROJECT_CONTEXT.md** / **PROJECT_STATE.md**; **наступна сесія:** опційно додати явний тумблер автопідбір труб / фіксовані труби (ручне керування поверх `trunk_pipes_selected`).

## Нотатка сесії 2026-04-16

- **Ізолінії виливу:** підписи рівнів **Q** (л/г) на полотні (`dripcad_legacy`).
- **Магістраль за поливами:** у кеші та підписі насоса — **оцінка мінімального H на джерелі** для заданого напору насоса (`trunk_irrigation_schedule_hydro`, `dripcad_legacy`); тест у `tests/test_trunk_irrigation_schedule_hydro.py`.
- **Панель «Магістраль»:** поле **«Індикація v ≥»** — збереження в `consumer_schedule` / JSON, підсвітка **v ≥ порогу** на полотні та карті; виправлено скидання поля під час набору (геттер порогу без `normalize_consumer_schedule`, синхронізація після `load_project`).

## Нотатка сесії 2026-04-17

- **Явний режим магістралі (авто/фікс):** у вкладці `Розклад` додано чекбокс **«Фіксовані труби (без автопідбору)»**, який напряму задає `consumer_schedule.trunk_pipes_selected`.
- **Динамічна телескоп-оптимізація:** `money`/`weight`, peak Q(t), телескоп 1–4 секції на ребро (суміжні діаметри), валідація `price_per_m` для `money`, сумісність `telescoped_sections` у гідравліці та I/O; ітерація ΔH у `run_trunk_irrigation_schedule_hydro`; тести в `test_pipe_weight_optimizer.py`, `test_trunk_irrigation_schedule_hydro.py`, `test_trunk_tree_compute.py`.
- **Розрахунок магістралі:** у `run_trunk_irrigation_schedule_hydro` прапорець режиму тепер застосовується з UI перед запуском; після автопідбору більше немає автоперемикання в fixed-mode.
- **Збереження/завантаження:** у `file_io_impl.py` додано флаш/синк стану нового перемикача (`_flush_schedule_trunk_pipe_mode_to_app`, `_sync_schedule_trunk_pipe_mode_ui`).
- **Емітери (некомпенсовані):** модель переведено на `q = k·H^x` без використання параметра `H опорна` у розрахунку; поле `H опорна` прибрано з вкладки «Гідравліка», у звіті оновлено формулювання.
- **Магістраль (розклад, оптимізація):** додано режим цілі `money/weight` (legacy `cost_index` → `money`), підтримку `price_per_m` в оптимізаторі, `total_objective_cost` у результаті та параметр `trunk_schedule_max_sections_per_edge` (1..4) у `consumer_schedule`.
- **Телескоп ребра:** оптимізатор формує `sections` для ребра з урахуванням мінімальної довжини секції; `trunk_tree_compute` рахує втрати по сумі секцій ребра.

## Нотатка сесії 2026-04-17 (вечір — UX магістралі)

- **Перетягування вузлів** у **VIEW** на полотні та карті; топологія (`node_indices`) без змін, оновлюється `path_local` / координати.
- **«Вибір» + рамка:** клік по вузлу магістралі не запускає marquee; рух вузла в `_canvas_b1_motion` — перед оновленням рамки.
- **Споживач і кран:** снап при постановці інструментом; під час drag без снапу; після відпускання — один снап; пунктирні **кола зон** навколо точок `get_valves()` (полотно + карта).
- **Труби магістралі:** кнопка вибору набору на вкладці **«Магістраль (HW)»**; покращений подвійний ЛКМ по відрізку (полілінії, пріоритет вузол/ребро за відстанню).
- **Видалення вузла/ребра:** лише **ПКМ-меню**; **Delete/BackSpace** не чіпають `trunk_node`/`trunk_seg` (лише `scene_line` та інша логика полотна).
- Оновлено **PROJECT_CONTEXT.md** (§7 snapshot) та цей файл.

## Нотатка сесії 2026-04-18

- **Телескоп магістралі:** slack ΔH у `trunk_irrigation_schedule_hydro`; порядок секцій апстрім→даунстрім у `pipe_weight_optimizer`; підписи на полотні; **LBL** перетягує також підписи телескопа (`trunk_telescope_label_pos`); виправлено відображення Ø (110/90, не 11/9).
- **UI:** історично **H пікет** та **Vmax≥** були перенесені з лівої «Магістраль» на **«Магістраль (HW)»**; станом на 2026-04-27 цей старий блок прибрано, а вставка пікета виконується через ПКМ на графіку тиску.
- Оновлено **PROJECT_CONTEXT.md** (§7 знімок 2026-04-18) та цей файл.

## Нотатка сесії 2026-04-19

- **Полотно магістралі:** підписи секцій з **Ø**; трикутник **45°** на стику секцій телескопа (до меншого d у бік потоку); теги **`trunk_map_BOM`** / **`trunk_map_Cosmetic`** поверх кореня **`trunk_map_canvas`**; косметика піднімається над BOM.
- **ПКМ по ребру:** пункт **«Графік тиску вздовж ребра»** — `open_trunk_segment_pressure_profile`: профіль **H(s)** по **HW-секціях** з `trunk_tree` і кешу слота (`lateral_drip_core.hazen_williams_hloss_m`); **s** по полілінії; рух миші на графіку синхронізує **`_set_trunk_profile_probe`** на трасі (як «Профіль прокладки»); вертикальний маркер на діаграмі.
- **Документація:** оновлено **PROJECT_CONTEXT.md** (знімок 2026-04-19) та цей файл.

## Нотатка сесії 2026-04-19 (доповнення)

- **Розклад — напір насоса:** значення **0** у полі «Напір насоса (задано)» підставляє **орієнтовний мінімальний H** за однорідною **найтовстішою** дозволеною трубою магістралі (`estimate_min_pump_head_m_uniform_largest_allowed_pipe`, UI у `control_panel_impl`, метод на **`DripCAD`**); збереження з 0 у полі — та сама логіка у `_flush_schedule_max_pump_head_to_app`; **`max_pump_head_m: 0`** у JSON більше не губиться при завантаженні (`file_io_impl`); **`normalize_consumer_schedule`** допускає **0…400** до підстановки; **`_sync_schedule_max_pump_head_ui`** показує **«0»**.
- **Синхрон магістраль карта → дерево:** **`_trunk_non_empty_section_list_from_seg`** — порядок **`sections`**, потім **`telescoped_sections`** (`dripcad_legacy`), щоб не відкочувати телескоп до однієї секції через застарілий `telescoped_sections`.
- **Тести:** `test_estimate_min_pump_head_uniform_largest_allowed_pipe` у `tests/test_trunk_irrigation_schedule_hydro.py` (кандидати труб у модулі гідравліки поливів без імпорту `hydraulics_core`).
- Оновлено **PROJECT_CONTEXT.md** (другий знімок за 2026-04-19) та цей файл.

## Нотатка сесії 2026-04-20 (магістраль: геодезія в бюджеті, антидефіцитний цикл)

- **UI — миттєва автопідстановка H насоса:** `ent_max_pump` на вкладці **«Розклад»** тепер слухає `<Return>` і `<FocusOut>` (`control_panel_impl`): введення `0` одразу підставляє оцінку за найтовстішою дозволеною трубою (`estimate_min_pump_head_m_uniform_largest_allowed_pipe`). Кнопку розрахунку перейменовано в **«▶ Оптимізація»**.
- **Мікрохвости телескопа:** у `modules/hydraulic_module/trunk_irrigation_schedule_hydro.py` додано `_collapse_short_telescope_sections` (константа `_TRUNK_MICRO_SECTION_MERGE_M = 5.0`): будь-яка секція з `L ≤ 5 м` «доклеюється» до upstream-сусіда, сумарні `head_loss_m` / `weight_kg` / `objective_cost` зшиваються. Викликається в `optimize_trunk_diameters_by_weight` і `refine_trunk_picks_pressure_tightening`.
- **Стійкий антидефіцитний цикл** у `_run_trunk_irrigation_optimize_loop` (`dripcad_legacy.py`):
  - `dh` для ітерацій бюджету тепер береться як **`dh_worst`** з активних слотів (не default-значення `trunk_schedule_test_h_m`).
  - Бюджет при дефіциті **стискається** (`tighten = max(0.45, 1 − worst_def/dh)`), а не розширюється; 8 ітерацій замість 4.
  - `last_cache_ok` фіксується **лише** при нульовому дефіциті; дефіцитні варіанти трекаються окремо в `best_def_cache/best_def_tree` (найменший дефіцит + менша метрика при рівності).
  - Якщо жодна ітерація не дала чистого рішення, завжди вмикається fallback на **uniform largest** і порівнюється з `best_def_cache`; якщо fallback кращий — застосовується, інакше відкочуємося до найкращого дефіцитного телескопа через `_sync_trunk_segment_hydraulic_props_from_tree`.
- **Геодезичний dz у бюджеті** — **ключове виправлення** стабільності автопідбору:
  - Перед циклом робляться два пробні `compute_trunk_irrigation_schedule_hydro` на **uniform largest** з `pump = mph + 1000` (щоб уникнути прикордонних попереджень): один із `surface_z_at_xy=surf_z`, інший — без.
  - `dz_eff = hc_no_z − hc_with_z` — **алгебраїчне** (зі знаком): `+` підйом (споживач вище джерела) звужує робочий бюджет, `−` спуск розширює його, щоб optimizer вибрав легший телескоп, який скидає надлишок саме до цілі.
  - Робочий бюджет: `budget = max(0.1, max_hloss_budget_m − dz_eff)` з проградиційним рядком у прогрес-міт́ці: `Магістраль поливів: геодезичний підйом/спуск ΔZ≈±X.XX м → робочий бюджет ΔH≤Y.YY м`.
- **Результат тестування на `designs/atest0/atest0.json`:** переміщення споживача у різні положення (підйом +2.5 м / спуск −6 м) дає телескопи з `H_споживача ≈ target (10 м)` без дефіциту; вибір мінімальний за вагою, mph у полі не «стрибає» ітеративно між значеннями.
- **Автотест регресії `dz`:** у `tests/test_trunk_irrigation_schedule_hydro.py` додано `test_synthetic_dz_keeps_min_consumer_head_near_target` — synthetic `surface_z_at_xy` для `dz ∈ {-6, 0, +2.5}`, перевірка `abs(min_consumer_head_m - target_head_m) <= 0.5` після підбору труб + повного `compute_trunk_irrigation_schedule_hydro`.
- **Прогін тестів (локально):** `python -m unittest tests.test_trunk_irrigation_schedule_hydro` → **OK** (`13 tests`), окремо `...test_synthetic_dz_keeps_min_consumer_head_near_target` → **OK** (`1 test`).
- **Файли:** `main_app/ui/control_panel_impl.py`, `main_app/ui/dripcad_legacy.py`, `modules/hydraulic_module/trunk_irrigation_schedule_hydro.py`.

## Нотатка сесії 2026-04-21 (резервні джерела висот, верхня панель)

- **SRTM/DEM fallback:** у `modules/geo_module/topography_core.py` для missing точок після локального `_srtm_` додано ланцюг резервів **`Open-Elevation -> NASA Earthdata`** (базовий шлях лишається локальний `skadi_local`); у режимах primary (`open_elevation` або `earthdata`) при недоступності джерела виконується автоматичний перехід до наступного.
- **Earthdata (опційно):** резерв активується через env `EARTHDATA_ELEVATION_API_URL`, `EARTHDATA_USER`, `EARTHDATA_PASSWORD`; якщо не налаштовано — джерело м’яко пропускається, без крашу UI.
- **UI (верхня панель):** у `main_app/ui/dripcad_legacy.py` додано дропліст джерела висот (`Авто`, `Skadi+локальні`, `Open-Elevation`, `NASA Earthdata`) + індикатор фактично активного джерела.
- **Пайплайн виклику:** `main_app/ui/app_ui.py` -> `main_app/orchestrator.py` -> `modules/geo_module/engine.py` -> `topography_core.fetch_srtm_grid(..., source_mode=...)`; в повідомленні успіху показується активний provider.
- **Збереження/завантаження:** у `main_app/io/file_io_impl.py` нормалізується і зберігається `consumer_schedule.srtm_source_mode`; після `load_project` відновлюється дропліст/стан верхньої панелі.

## Нотатка сесії 2026-04-22 (SRTM tiles, EarthData, ізолінії, UI рельєфу)

- **`srtm_tiles`:** завантаження тайлів через **`earthaccess`** (SRTMGL1 LP DAAC), якщо немає `EARTHDATA_SRTM_TILE_BASE`; **`configure_earthdata_tk_bridge`** + модальний діалог логін/пароль при невдалому вході; стратегії `login` без stdin з фонового потоку в GUI (`environment` / `netrc`).
- **`paths.py`:** `EarthData.txt` → env; синхрон **USER** ↔ **USERNAME** для earthaccess.
- **`.gitignore`:** `EarthData.txt` (варіанти).
- **`app_ui` / `control_panel_impl`:** кнопка **«Тайли»** — лише тайли за зоною проєкту (`download_srtm_tiles_for_project_zone`).
- **`topography_core`:** згладжування сітки Z перед ізолініями; `_fetch_earthdata_batch` + файл кредів.
- **GitHub:** коміти `82e63d2` (ширший пакет), `fe4977d` (Earthdata UI/USERNAME/діалог).

## Нотатка сесії 2026-04-22 (магістраль, графіки, зум, стан чекбоксів)

- **Магістраль (логічне ребро через пікети):** у `modules/hydraulic_module/trunk_irrigation_schedule_hydro.py` ланцюг `source→...→consumption` з `bend` оптимізується як один телескоп на сумарну довжину; фізичні сегменти отримують лише «укладання» секцій по трасі без локальної переоптимізації.
- **Візуалізація магістралі:** у `main_app/ui/dripcad_legacy.py` виправлено колір/підпис секцій телескопа (в т.ч. для односекційного ребра), щоб фактичні Ø з розрахунку відмальовувались коректно.
- **Графік латералі:** синхронізовано стартову точку тиску у врізці для L1/L2 (один `H_submain_conn_m`), додано hover-пояснення по лініях та підписи для всіх горизонтальних пунктирів (`Hmin/Hmax`, `Q@Hmin/Q@Hmax`), легенду закріплено в правому верхньому куті.
- **Адаптивний розмір графіків:** `show_graph`/`show_submain_graph` тепер рахують область побудови від реальних `canvas` розмірів; при resize вікна графік дебаунсно перемальовується (`_schedule_graph_redraw`).
- **Зум/рельєф стан проєкту:** додано кнопку **«Зум візібл»** (верхня панель); у `file_io_impl` серіалізується та відновлюється стан чекбоксів рельєфу: `show_contours`, `show_topo_points`, `show_topo_computation_zone`, `show_srtm_boundary_overlay`.

## Нотатка сесії 2026-04-22 (фіксація: LKM-панорама «без карти», вилив у «Блок»)

- **Полотно «без карти»:** панорама в `VIEW/PAN` працює як drag ЛКМ (без колеса). У `dripcad_legacy.py` оновлено ланцюг `ButtonPress-1`/`B1-Motion`/`ButtonRelease-1` для `start_pan`/`handle_pan`/`end_pan`.
- **Сумісність інструментів:** `select` (рамка), перетягування вузла магістралі, `ZOOM_BOX` і спеціальні canvas-інструменти лишають свій пріоритет; панорама не перехоплює їхні жести.
- **Вилив/маски:** важкий рендер виливу не малюється на головному canvas; відображення перенесено в панель вкладки «Блок», redraw панелі не прив’язаний до глобального redraw (менше фризів).
- **Стан репозиторію:** за запитом користувача зроблено лише фіксацію контексту (документація), без нових архітектурних змін і без коміту.

## Нотатка сесії 2026-04-23 (WIP доробка: K_eq, аудит Q, path_local)

- **Новий модуль:** `modules/hydraulic_module/emitter_block_equivalent.py` — pure functions для еквівалентного емітера блоку (`equivalent_k_at_ref`, `equivalent_k_from_total_flow`, `block_flow_at_ref`, `mean_positive`) з docstring про припущення (`q = k*H^x`, спільний `x`, опорний `p_ref`).
- **HydraulicEngine:** у `modules/hydraulic_module/hydraulics_core.py` додано:
  - `calc_results["lateral_flow_audit"]` (Q min/max/nominal, статуси `ok_q|overflow_q|underflow_q|both_q|no_emitters`, мін/макс емітер по латералі),
  - `calc_results["block_equivalent_emitter"]` (per block: `k_eq_lph_mx`, `x_exp`, `p_ref_m`, `q_total_lph`, `n_emitters`, джерело `p_ref`),
  - підтримку очищення/ремапу/merge `lateral_flow_audit` при block-local скиданнях і частковому перерахунку.
- **Уточнення по `K_eq` (2026-04-23):** `x` для `block_equivalent_emitter` береться тільки з уже обраної моделі крапельниці (`emitter_opts.x_exp`), fallback `x=0.5` прибрано; додано поле `x_source="selected_dripper_model"`. Тобто в цьому кроці **не виконується підбір `x`**, обчислюється лише `K_eq`.
- **Вікно «Властивості блоку»:** у `main_app/ui/dripcad_legacy.py` (`open_block_irrigation_scheme_dialog`) додано 2 read-only поля перегляду після розрахунку: **`K_eq (для q=K·H^x)`** та **`P_ref (м)`** з `calc_results["block_equivalent_emitter"][block_idx]`; без розрахунку показується повідомлення, що дані з’являться після «Розрахунок».
- **Вікно «Властивості блоку» (уточнення):** додано read-only поле **`x (ступінь)`** поруч із `K_eq`/`P_ref`, щоб було видно повну пару параметрів еквівалентного емітера.
- **Вікно «Властивості блоку» (доповнення):** додано поле вводу **`Тиск для оцінки Q_total (м)`** та read-only поле **`Q_total @ H (л/г)`**; значення перераховується live за `Q = K_eq * H^x` для активного блоку (якщо `K_eq/x` валідні, інакше `—`).
- **Вікно «Властивості блоку» (% до Q_nom):** додано read-only поле **`% до Q_nom блоку`**; обчислення live: `((Q_total@H / (n_emitters * Q_nom_emit)) - 1) * 100%`, де `Q_nom_emit` — поточне значення поля «Q номінал».
- **Надійність заповнення K_eq/x:** у `app_ui._collect_hydro_dto` додано fallback параметрів емітера `k/x/kd` із `_dripper_record` (за `emit_model` + `emit_nominal_flow`), якщо `x` у глобальному полі порожній/нульовий; у `open_block_irrigation_scheme_dialog` при `Застосувати` для активного блоку ці `k/x/kd` також підтягуються в глобальні змінні.
- **Фікс для порожніх полів K_eq/x після active-block розрахунку:** у `dripcad_legacy` виправлено remap/merge block-level ключів для часткового перерахунку (`_remap_partial_hydro_results(..., orig_block_idx)` + merge `block_equivalent_emitter`), а також очищення цього ключа у block-local strip/clear. Після цього `Властивості блоку` читають коректний `calc_results["block_equivalent_emitter"][block_idx]`.
- **ПКМ по блоку на полотні:** у `VIEW/PAN` додано `context menu` блоку з попаданням **по площі блоку** (щоб не цілитись у контур): `Властивості блоку…`, `Зробити активним блок`, `Оновити сітку блоку`, `Очистити гідравліку блоку`, `Видалити блок…` з підтвердженням.
- **Термінологія у властивостях блоку:** підпис поля змінено на **«Модель латералі (довідник)»** (було «Модель труби»), щоб збігалось із контекстом секції «Латераль».
- **Проміжні підказки drag-вузла:** службовий текст довжини ребра (`100.0 м` тощо) під час перетягування вузла магістралі на полотні вимкнено; залишилась лише геометрична поведінка перетягування без виводу проміжних чисел.
- **Вкладка «Блок»:** з секції `Діаграма виливу (прев’ю)` прибрано 2 чекбокси (`Ізолінії виливу`, `Маски переливу / недоливу...`) — інтерфейс очищено від зайвих перемикачів.
- **UX снапу/підписів магістралі:** радіус снапу до вузлів-кранів зменшено вдвічі (`_PICK_FIELD_VALVE_R_M: 22 -> 11`); пунктирний overlay снапу більше не лишається світитись постійно (показ лише в релевантних сценаріях); при drag вузла приховується тільки дрібний підпис сегмента (`ЛФ... 100м`), великий підпис сегмента/телескопа залишено.
- **Дрібні підписи прибрано повністю:** вимкнено постійні дрібні підписи сегментів на полотні й карті (включно з проміжною довжиною ребра під час drag на карті); залишено лише фінальні підписи секцій телескопа після оптимізації.
- **Snap вузлів графа:** радіус снапу піднято приблизно в 1.5 раза від поточного (`_PICK_FIELD_VALVE_R_M: 11 -> 16.5`) для комфортнішого захоплення вузлів.
- **Snap вузлів графа (уточнення):** зменшено фактичну зону снапу до вузлів магістралі: `_TRUNK_NODE_SNAP_CANVAS_M` з 16 до 11 і екранний поріг у `_trunk_snap_radius_m` з ~14 px до ~10 px (щоб зона не виходила далі візуальної червоної мітки).
- **Snap вузлів графа (жорстко над об’єктом):** прибрано залежність радіуса snap від zoom; `_trunk_snap_radius_m` тепер повертає фіксований радіус `11 м`, щоб зона не розширювалась при зміні масштабу.
- **Snap до кранів (жорсткий):** `_snap_world_xy_to_nearest_field_valve` переведено на фіксований допуск через `_consumer_valve_snap_radius_m`, а сам `_consumer_valve_snap_radius_m` тепер повертає константу `_PICK_FIELD_VALVE_R_M` (без додаткового `px`-масштабування від zoom).
- **ПКМ multi-pick:** додано єдиний обробник `_open_context_menu_for_world_pick` (canvas/map): при одному попаданні відкривається меню цього об’єкта, при кількох — з’являється список кандидатів для вибору конкретного об’єкта під курсором; підтримано таргетоване відкриття меню для `trunk_node`, `trunk_seg`, `block`.
- **Візуальний snap-радіус вузла графа:** додано підсвітку кола фактичного радіуса прив’язки при наближенні до вузла (`_draw_trunk_node_snap_radius_hint_on_canvas` + аналог у `map_viewer_tk_window._draw_trunk_map_node_glyphs`).
- **Snap-зона вузла зменшена:** `_TRUNK_NODE_SNAP_CANVAS_M` зменшено утричі (`11.0 -> 3.67`); підсвітка кола радіуса snap збережена.
- **UI / відображення:** у `main_app/ui/control_panel_impl.py` додано панель **«Вилив vs Q ном (±5 %)»**; у `main_app/ui/dripcad_legacy.py` реалізовано `refresh_block_lateral_flow_audit_panel`; у `main_app/ui/map_viewer_tk_window.py` колір латералей пріоритетно враховує `lateral_flow_audit` (з fallback на pressure-audit).
- **Профіль магістралі:** `_trunk_segment_world_path` у `dripcad_legacy.py` для двовузлового ребра повертає `path_local`, якщо вона валідна/детальніша за хорду (з корекцією напряму), щоб графіки/hover йшли по реальній ламаній траси.
- **Тести:** додано `tests/test_emitter_block_equivalent.py` та `tests/test_trunk_segment_world_path.py`; локальний прогін (2026-04-23):  
  - `py -m unittest tests.test_emitter_block_equivalent` → **OK** (5 tests)  
  - `py -m unittest tests.test_trunk_segment_world_path` → **OK** (3 tests)

## Нотатка сесії 2026-04-24 (властивості блоку, Hвст., ПКМ споживача)

- **Вікно «Властивості блоку»:** у секції еквівалентного емітера поле оцінки тиску замінено на editable **`Hвст. (встановлений тиск, м)`**; значення зберігається в `field_blocks[i]["params"]["h_set_m"]`, використовується для live-оцінки **`Q_total @ H`** та **`% до Q_nom`**.
- **Вікно «Властивості блоку» (доповнення):** додано read-only поле **`H для Q_nom блоку (м)`** як обернену оцінку з `Q = K_eq * H^x`, а також окрему кнопку **`▶ РОЗРАХУНОК`** прямо внизу діалогу (`open_block_irrigation_scheme_dialog`).
- **Ключовий UX-фікс:** кнопки **`OK`** / **`Застосувати`** у «Властивостях блоку» більше **не скидають гідравлічний розрахунок**, якщо користувач не змінив параметри, що реально впливають на геометрію/гідравліку. Причина була в `trace_add(... reset_calc ...)` на глобальних `StringVar`; синхронізація цих `var_*` тепер виконується тільки коли справді потрібен `need_recalc`.
- **ПКМ по споживачу магістралі:** у контекстному меню вузла `consumption/valve` додано пункт **`Властивості…`**, який відкриває наявний діалог параметрів споживача (`_open_trunk_consumer_schedule_dialog`) для Q та цільового H.

## Нотатка сесії 2026-04-24 (canvas layers, сабмейн Ø-шари, підписи кранів)

- **Canvas layer manager:** у `main_app/ui/dripcad_legacy.py` додано окремий менеджер шарів полотна з групами **Блок / Магістраль / Косметика**, діалогом **`🗂 Шари...`**, викликом з **ПКМ по порожньому canvas** і правилами: лише видимий шар може бути `selectable`, увімкнення `selectable` автоматично вмикає `visible`.
- **Збереження стану шарів:** `main_app/io/file_io_impl.py` серіалізує `params["canvas_layers"]`; після `load_project` видимість/селектабельність шарів та динамічних `Ø`-шарів відновлюється автоматично.
- **Шари блоку деталізовані:** блоковий шар сабмейну розбитий на **`block.submain.base`**, **`block.valves`** і динамічні **`block.submain.od:*`** по діаметрах секцій; окремо лишаються **`block.boundaries`** і **`block.laterals`**. У магістралі діють **`trunk.graph.nodes`**, **`trunk.graph.edges`** і динамічні **`trunk.pipes.od:*`**; підписи контролюються через **`cosmetic.labels`**.
- **Рендер і hit-test пов’язані з шарами:** `redraw`, `_collect_world_pick_hits`, `_pick_hits_in_world_rect`, вибір вузлів/ребер/блоків і порожнього полотна тепер поважають `visible/selectable`; сабмейн можна вибирати або як базову геометрію, або через selectable `Ø`-шар відповідної секції.
- **Підписи кранів поля:** підпис клапана зроблено **багаторядковим**, **left-aligned**, з темною підкладкою та **двокроковим drag** у `SUB_LABEL`; положення пишеться в `consumer_schedule.field_valve_label_pos`.
- **Qmin/Qmax по гілках:** `_emitter_q_extrema_overlay` тепер зберігає екстремуми **окремо по `branches[sm_idx]`**, а `redraw()` показує мін/макс вилив не лише по блоку, а й **по кожній гілці сабмейну**.

## Нотатка сесії 2026-04-24 (рефакторинг contracts / I/O / hydraulic API)

- **Orchestrator contracts:** додано `main_app/contracts/orchestrator_models.py` (`HydraulicRunSnapshot`, `HydraulicResultsSnapshot`, `BomSnapshot`). `main_app/orchestrator.py` нормалізує `last_hydraulic`, `last_bom`, `last_stress` через ці snapshots; для UI лишається сумісний словниковий контракт.
- **I/O split без зміни поведінки:** з `main_app/io/file_io_impl.py` винесено JSON helpers (`project_serialization.py`), нормалізацію `consumer_schedule` (`project_normalizers.py`), save/restore блоків (`project_blocks.py`) і підготовку trunk payload (`project_trunk.py`). `file_io_impl.py` лишається основним entrypoint для load/save/export.
- **Сумісність форматів:** зафіксовано правило — старий JSON має завантажуватись через normalizer/fallback, а збереження пише новий канонічний формат. Проєкти в `designs/` використовуються як тестові fixtures і не мають ламатися без міграції.
- **Hydraulic façade:** додано `modules/hydraulic_module/api.py`; `dripcad_legacy.py` імпортує UI-facing helper-и через façade, а не напряму з `hydraulics_core`.
- **Регресія load_project:** після винесення trunk payload повернуто імпорт `ensure_trunk_node_ids` у `file_io_impl.py`; додано `tests/test_file_io_imports.py`.
- **Focused tests:** локально `py -m pytest tests/test_file_io_imports.py tests/test_hydraulic_api.py tests/test_project_trunk.py tests/test_project_blocks.py tests/test_project_normalizers.py tests/test_project_serialization.py tests/test_orchestrator_models.py tests/test_orchestrator_memory_trim.py` → **18 passed**.

## Нотатка сесії 2026-04-26

- **Документація / memory-bank:** оновлено **PROJECT_CONTEXT.md** (додано знімок 2026-04-26 про синхронізацію з `memory-bank/`), **PROJECT_STATE.md** (цей футер і нотатка), а також `memory-bank/activeContext.md` та `memory-bank/progress.md` для узгодження дати останнього вирівнювання з репозиторієм.
- **Контекст коду:** на момент оновлення у робочому дереві залишались незакомічені зміни в `orchestrator`, `file_io_impl`, I/O helper-модулях, UI, `modules/hydraulic_module` і нові/доповнені тести в `tests/`; орієнтир по функціоналу — нотатки **2026-04-24** (contracts / I/O split / `api.py`) і **2026-04-23** (K_eq, аудит Q) у цьому файлі та в **PROJECT_CONTEXT.md** §7.

## Нотатка сесії 2026-04-26 (UI: вибрані блоки)

- **Властивості вибраних блоків:** у `main_app/ui/dripcad_legacy.py` виправлено збір scope для multi-block selection. `_selected_block_indices()` тепер враховує не тільки явні `block`, а й вибрані `submain` / `lateral`, прив'язані до своїх блоків.
- **Dropdown у діалозі:** контекстне меню **«Властивості вибраних блоків…»** передає snapshot повного списку блоків у `open_block_irrigation_scheme_dialog`; при active multi-selection комбобокс **Блок:** стартує з `Всі вибрані: ...` і показує всі блоки scope, а не тільки перший.
- **Скидання selected:** selection на canvas очищується при закритті діалогу (`OK`, `Скасувати`, хрестик), а не одразу після відкриття, щоб вікно параметрів встигло стабільно прочитати повний список вибраних блоків.
- **Перевірка:** `ReadLints` для `main_app/ui/dripcad_legacy.py` — без помилок; `python -m py_compile main_app/ui/dripcad_legacy.py` — OK.

## Нотатка сесії 2026-04-26 (магістраль HW: display H та підписи труб)

- **Display H після автопідбору:** у `modules/hydraulic_module/trunk_irrigation_schedule_hydro.py` додано `use_required_source_head_per_slot`. Після оптимізації труб `main_app/ui/dripcad_legacy.py` формує кеш відображення з мінімальним потрібним H джерела для кожного слота, тому hover показує регульований тиск слота, а не глобальний worst-case насос.
- **Регресія `atest`:** для `designs/atest/atest.json` споживач **C2/T6** у поливі 2 тепер у display-режимі дає `source≈22.45 м`, `H≈17.00 м` замість `H≈50.60 м` при `max_pump_head_m=55`.
- **Підписи труб магістралі:** hover/pick підпис труби у режимі труб переведено на формат **`ABBR ØOD/PN Lм`** через `_format_pipe_signature` (`ПВХ Ø150/6 629.2м` або `ABBR` з `pipes_db`).
- **Перевірка:** `c:\DRD\.venv\Scripts\python.exe -m pytest tests/test_trunk_irrigation_schedule_hydro.py` → **16 passed**; `c:\DRD\.venv\Scripts\python.exe -m py_compile main_app/ui/dripcad_legacy.py` → OK; `ReadLints` для змінених файлів — без помилок.
- **Операційно:** запущений Tk-процес треба перезапустити й заново натиснути **«Оптимізація»**, щоб побачити новий кеш/підписи.

## Нотатка сесії 2026-04-27 (магістраль: полілінія, графік тиску, пікет з графіка)

- **Траса без автопікетів:** у режимі `trunk_route` ЛКМ у вільній точці більше не створює `bend`; він додає лише проміжну точку чернетки полілінії. Пікет створюється тільки явною командою **«Пікет»** або з графіка тиску.
- **Збереження полілінійного ребра:** після ПКМ завершення траси `path_local` не випрямляється у хорду. `sync_trunk_segment_paths_from_nodes()` для двовузлового ребра зберігає валідну полілінію і лише прив'язує її першу/останню точки до вузлів.
- **Графіки тиску:** ПКМ по ребру тепер має два різні пункти: **«Графік тиску вздовж ребра»** (тільки вибране ребро) і **«Графік тиску вздовж гілки від насоса…»** (повний шлях гілки). Для полілінійного ребра профіль H/ψ рахується по довжині всієї ламаної.
- **Вставка пікета з графіка:** у вікні графіка тиску ПКМ по canvas відкриває дію **«Вставити пікет тут»**. Координата X перетворюється в `s`; для гілки додатково знаходиться конкретне ребро та локальна відстань. Ребро ріжеться на два `path_local`, додається `bend`, `trunk_irrigation_hydro_cache` скидається.
- **Cleanup правої панелі:** з вкладки **«Магістраль (HW)»** прибрано старий блок **«На полотні / карті»** з `H, м`, **`Пікет @ H`** і **`Vmax≥`**; `ControlPanel.var_trunk_picket_head_m` видалена.
- **Перевірка:** `ReadLints` для `main_app/ui/dripcad_legacy.py` і `main_app/ui/control_panel_impl.py` — без помилок; `py -m py_compile main_app/ui/dripcad_legacy.py` і `py -m py_compile main_app/ui/control_panel_impl.py` — OK.

*Останнє оновлення опису: 2026-04-27 — полілінійні ребра магістралі, графік тиску ребро/гілка, ПКМ-вставка пікета з графіка, cleanup правої панелі «Магістраль (HW)», синхронізація PROJECT_CONTEXT / PROJECT_STATE / memory-bank.*
